import Vue from "vue";
import VueSweetalert2 from 'vue-sweetalert2';

// If we do custom config, we do it like so:
// const options = {
//   confirmButtonColor: "#41b882",
//   cancelButtonColor: "#ff7674",
// };
// Vue.use(VueSweetalert2, options);

Vue.use(VueSweetalert2);